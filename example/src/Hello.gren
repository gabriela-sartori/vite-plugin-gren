module Hello exposing (main)

import Browser
import Html
import Message


main =
    Html.text <| "Hi!!!!" ++ Message.importableMessage



-- Browser.sandbox
--     { init = ""
--     , update = \_ _ -> ""
--     , view = \_ -> Html.text "Sandbox is not working - yet"
--     }
